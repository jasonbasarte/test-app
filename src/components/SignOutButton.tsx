'use client';

const SignOutButton = () => {
  return (
    <button
      className="border-none text-gray-700 hover:text-gray-900"
      type="button"
    >
      Sign out
    </button>
  );
};

export { SignOutButton };
